import { DebounceClickUserDirective } from './debounce-click-user.directive';

describe('DebounceClickUserDirective', () => {
  it('should create an instance', () => {
    const directive = new DebounceClickUserDirective();
    expect(directive).toBeTruthy();
  });
});
