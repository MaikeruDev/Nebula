import { DebounceClickProfileDirective } from './debounce-click-profile.directive';

describe('DebounceClickProfileDirective', () => {
  it('should create an instance', () => {
    const directive = new DebounceClickProfileDirective();
    expect(directive).toBeTruthy();
  });
});
