import { DebounceClickPostDirective } from './debounce-click-post.directive';

describe('DebounceClickPostDirective', () => {
  it('should create an instance', () => {
    const directive = new DebounceClickPostDirective();
    expect(directive).toBeTruthy();
  });
});
