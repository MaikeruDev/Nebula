import { DebounceClickFollowSuggestionDirective } from './debounce-click-follow-suggestion.directive';

describe('DebounceClickFollowSuggestionDirective', () => {
  it('should create an instance', () => {
    const directive = new DebounceClickFollowSuggestionDirective();
    expect(directive).toBeTruthy();
  });
});
